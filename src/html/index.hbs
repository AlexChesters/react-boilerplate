<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>My React Redux Bootstrap Boilerplate</title>
  </head>
  <body>
    <script>
      (_ => {
        const path = window.location.pathname
        if (path === '/') {
          return
        }
        if (path.endsWith('/')) {
          location.assign(window.location.origin + path.substr(0, path.length - 1))
        }
      })()
    </script>
    <script>
     history.pushState({}, "entry page", location.hash.substring(1));
    </script>
    <div id="app"></div>
  </body>
</html>
